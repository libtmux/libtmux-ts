{
  "name": "@example/fields",
  "version": "0.0.0",
  "private": true,
  "description": "Read tmux's values decoded and typed, without parsing. Never published.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test --no-orphans ."
  },
  "dependencies": {
    "libtmux": "workspace:*"
  }
}
