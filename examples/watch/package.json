{
  "name": "@example/watch",
  "version": "0.0.0",
  "private": true,
  "description": "Follow tmux's own notifications instead of polling. Never published.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test --no-orphans ."
  },
  "dependencies": {
    "libtmux": "workspace:*"
  }
}
