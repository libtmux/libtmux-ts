{
  "name": "@example/workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Build a tmux workspace, by hand or from a declared layout. Never published.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test --no-orphans ."
  },
  "dependencies": {
    "@libtmux/workspace": "workspace:*",
    "libtmux": "workspace:*"
  }
}
