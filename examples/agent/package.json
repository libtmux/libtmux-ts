{
  "name": "@example/agent",
  "version": "0.0.0",
  "private": true,
  "description": "Act on a pane and wait through a persistent observer. Never published.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test --no-orphans ."
  },
  "dependencies": {
    "libtmux": "workspace:*"
  }
}
