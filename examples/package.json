{
  "name": "@example/libtmux",
  "version": "0.0.0",
  "private": true,
  "description": "Runnable examples. Never published.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test --no-orphans tests"
  },
  "dependencies": {
    "libtmux": "workspace:*"
  }
}
