{
  "extends": "../libtmux/tsconfig.tooling.json",
  "compilerOptions": {
    "noEmit": true,
    // Bun's ambient declarations conflict with the pinned Node declarations.
    // The emitted and installed-package lanes check dependencies strictly.
    "skipLibCheck": true,
    "rootDir": "../..",
    "paths": {
      "libtmux": ["../libtmux/src/index.ts"],
      "libtmux/*": ["../libtmux/src/*.ts"]
    }
  },
  "include": ["src/**/*.ts", "tests/**/*.ts"]
}
